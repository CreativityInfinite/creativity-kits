import type { ToolMeta } from '~/types/tool';

const meta: ToolMeta = {
  id: 'regex-tester',
  name: 'tools.regex-tester.name',
  description: 'tools.regex-tester.desc',
  category: 'regex-parse',
  tags: 'tools.regex-tester.tags',
  color: '#f59e0b',
  icon: 'tabler:regex',
  entry: 'tools/regex-parse/regex-tester/Component.vue'
};
export default meta;
